import 'package:flutter/material.dart';

class AppColors {
  // Brand colors extracted from the new logo
  static const Color background = Color(0xFF000000); // Pure Black for seamless branding
  static const Color primary = Color(0xFF48C0F5);    // Vibrant Blue from the logo top
  static const Color secondary = Color(0xFF7C5CF0);  // Deep Purple from the logo bottom
  static const Color accent = Color(0xFF00E5FF);     // Electric Teal for highlights
  
  static const Color error = Color(0xFFFF5A5F);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B7C3);
  static const Color card = Color(0xFF121212);      // Slightly elevated black
}
